# Environment configuration for development
# Copy this file to .env and fill in the values

# Development server port (optional - defaults to 3000)
PORT=3000

# API endpoint - for production build, use the GCP Cloud Run URL
API=https://ircobi-papers-api-192399026075.europe-west2.run.app

# reCAPTCHA site key (get from Google reCAPTCHA console)
RECAPTCHA_SITE_KEY=

# reCAPTCHA bypass - DEVELOPMENT ONLY! NEVER SET IN PRODUCTION!
# This allows bypassing reCAPTCHA during local development
# IMPORTANT: This should NEVER be set when building for production deployment
# RECAPTCHA_BYPASS=your-dev-bypass-token

# Site identifier (optional)
SITE=

# Startup email configuration (optional)
STARTUP_EMAIL=
STARTUP_SUBJECT=
STARTUP_FROM=
